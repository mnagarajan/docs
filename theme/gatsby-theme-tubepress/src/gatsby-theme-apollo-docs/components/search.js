import React from 'react';
import Search from 'gatsby-theme-apollo-docs/src/components/search';

export const { boxShadow } = Search;
export default (props) => <Search {...props} siteName='the TubePress docs' />;
