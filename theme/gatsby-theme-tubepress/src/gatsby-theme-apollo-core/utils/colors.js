import { colors as spaceKitColors } from '@apollo/space-kit/colors';
import { colors as parentThemeColors } from 'gatsby-theme-apollo-core/src/utils/colors';

export const colors = {

  ...parentThemeColors,

  // primary links and anchors
  primary: spaceKitColors.blilet.base,
};
